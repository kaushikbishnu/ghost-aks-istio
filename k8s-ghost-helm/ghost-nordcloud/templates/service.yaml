apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghost-nordcloud.fullname" . }}
  labels:
    {{- include "ghost-nordcloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghost-nordcloud.selectorLabels" . | nindent 4 }}
